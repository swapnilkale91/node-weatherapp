<!DOCTYPE html>

<html>
    <head>
        <link rel="stylesheet" href="./css/styles.css">
    </head>
    <body>
        <h1>{{>header}}</h1>
        <h1>{{errorMsg}}</h1>
        <img src="/img/404.jpg"/>
        <h1>{{>footer}}</h1>
    </body>
</html>